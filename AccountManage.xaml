<Window x:Class="GPMI_Laser_Marking.AccountManage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GPMI_Laser_Marking"
        FontSize="15"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Account Manage" Height="550" Width="1024" Loaded="Window_Loaded">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBox" >
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="TextBlock" >
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
            <Style TargetType="CheckBox" >
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Margin" Value="20 0 0 0"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <Border BorderThickness="1" Margin="5" BorderBrush="Black" CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width=".8*"/>
                    <ColumnDefinition Width=".8*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="2.4*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock>ID</TextBlock>
                    <TextBox x:Name="ID_txt" Grid.Column="1" Margin="5 0"/>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="2.4*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock>Full Name</TextBlock>
                    <TextBox x:Name="FullName_txt" Grid.Column="1" />
                </Grid>
                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Margin="10 0 5 0">Password</TextBlock>
                    <TextBox Margin="5 11" x:Name="Password" Grid.Column="1" />
                </Grid>
                <CheckBox x:Name="Import_checkbox" Grid.Column="4" Grid.Row="0" Content="Import"/>
                <CheckBox x:Name="Manager_Account_checkbox" Grid.Column="0" Grid.Row="1" Content="Manager Account"/>
                <CheckBox x:Name="Marking_checkbox" Grid.Column="1" Grid.Row="1" Content="Marking"/>
                <CheckBox x:Name="FQA_checkbox" Grid.Column="2"    Grid.Row="1" Content="FQA Check"/>
                <CheckBox x:Name="Pakaging_checkbox" Grid.Column="3" Grid.Row="1" Content="Pakaging"/>
                <CheckBox x:Name="Shippingg_checkbox" Grid.Column="4" Grid.Row="1" Content="Shipping"/>

                <Button x:Name="Add_btn" Margin="20 10" Grid.Column="5" Click="Add_btn_Click">Add</Button>
                <Grid Grid.Column="5" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="Update_btn" Margin=" 10" Click="Update_btn_Click">Update</Button>
                    <Button x:Name="Delete_btn" Grid.Column="1" Margin=" 10" Click="Delete_btn_Click">Delete</Button>
                </Grid>
            </Grid>

        </Border>
        <DataGrid Margin="5" x:Name="Output_Grid" BorderThickness="2"  Grid.Row="2" FontSize="15" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionChanged="Output_Grid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="*" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Full Name" Width="*" Binding="{Binding FullName}"/>
                <DataGridTextColumn Visibility="Hidden" Header="Password" Width=".7*" Binding="{Binding Password}"/>
                <DataGridCheckBoxColumn Header="Import" Width=".5*" Binding="{Binding Import}"/>
                <DataGridCheckBoxColumn Header="Marking" Width=".5*" Binding="{Binding Marking}"/>
                <DataGridCheckBoxColumn Header="FQA" Width="0.5*" Binding="{Binding FQA}"/>
                <DataGridCheckBoxColumn Header="Pakaging" Width=".5*" Binding="{Binding Pakaging}"/>
                <DataGridCheckBoxColumn Header="Shipping" Width=".5*" Binding="{Binding Shipping}"/>
                <DataGridCheckBoxColumn Header="Account Manager" Width=".7*" Binding="{Binding Manager}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
